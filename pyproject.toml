[project]
name = "kabu-maker-taker"
version = "0.1.0"
description = "Python maker/taker microstructure strategy scaffold for kabu-style equities."
requires-python = ">=3.11"
dependencies = [
    "websocket-client>=1.8,<2",
]

[tool.ruff]
line-length = 110
